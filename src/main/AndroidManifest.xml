<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.oranda.libanius"
    android:debuggable="true">

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:debuggable="true">

        <activity android:label="@string/app_name" android:name=".QuizScreen"
                  android:configChanges="keyboardHidden|orientation"
                  android:screenOrientation="portrait" 
                  android:alwaysRetainTaskState="true"
                  android:windowSoftInputMode="stateHidden"
                  android:theme="@android:style/Theme.NoTitleBar"
                  />
        <activity android:label="@string/app_name" android:name=".OptionsScreen"
                  android:screenOrientation="portrait"
                  android:theme="@android:style/Theme.NoTitleBar"
                  >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

    <!-- Experimental: spoof as API level 8 (2.2) in order to persuade the HTC Desire C that
      it is compatible. In the build, the version must be 14 to support JDK 1.6. -->
    <uses-sdk android:minSdkVersion="8"
              android:targetSdkVersion="8"/>  <!-- later set these to 14 also -->
    <!--  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> -->
</manifest>

